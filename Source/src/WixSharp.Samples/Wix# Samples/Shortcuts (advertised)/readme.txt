This example (setup.cs) demonstrates how to install advertised shortut on Desktop.

You can also achieve the same by using Wix# generic Attributes functionality (setup2.cs).

Build msi by executing build.cmd. Then you can test setup by launching setup.msi.