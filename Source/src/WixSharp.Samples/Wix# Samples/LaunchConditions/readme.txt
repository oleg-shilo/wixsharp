This example demonstrates how use LaunchCondition to prevent the installation if .NET 2.0 is not installed on the target system
    	
Execute corresponding .cmd file to build desired msi. After msi is built execute it to test the setup.